<?php

namespace Wax_Dev;

class Event extends list {
    public function $Menu('Jobs');

}
if (!$player->isSneaking()) {
    if ($config->get($player->getName()) === "Jobs") {
        $lastutils = $this->cooldown[$player->getName()] ?? 0;
        $timeNow = time();
        if ($timeNow - $lastutils >= 15) {
            $player->sendMessage("§c[§fMenu des sorts§c] §2> §1Vous avez level up votre metier de ...");
            $this->cooldown[$player->getName()] = $timeNow;
        } else {

            if $Metier = list::server('jb');
//Metier//
            $menu->addbutton('Assassin ');
            $menu->addbutton('Minage');
            $menu->addbutton('Farmeur');

            if $sender->Menu = Open::class(/jobs);
            if $sender->lvl = open::class(/jobslvl)

            public clone id('menu');

            return
            $plugin->Caveblock(string: array){ bool;
                private function $nameItem <select name="CaveBlock" id="84">$Jobs</select>
            }

            public function $main\caveblock($Item, object = null);
    }
}